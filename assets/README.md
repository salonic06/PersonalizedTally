# Assets folder

| File | Purpose |
|------|---------|
| `invoice_template.xlsx` | Default invoice Excel template |
| `trash_row.png` | Small trash icon for table delete buttons (optional; Qt fallback if missing) |

UI illustrations (`dashboard_hero.png`, `empty_table.png`) are **not** used by the app.
