# Output Invoices (optional local scratch)

You can point **Settings → Invoice output folder** here or anywhere on disk. Generated `.xlsx` files are **not** committed to git (see `.gitignore`) so clones stay free of customer data.

Demo invoices after seeding live under `invoices/<financial-year>/` by default.
